"""Qt desktop UI (Task 14).

Nothing outside this package may import Qt, and nothing in it may contain
validation logic: the UI is a shell over `pipeline.run()`. `test_no_qt_imports`
enforces the first half mechanically.
"""
